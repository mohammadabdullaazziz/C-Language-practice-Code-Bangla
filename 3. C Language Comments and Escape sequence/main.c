#include <stdio.h>
int main()
{
    printf("My name is Abdullah\n");
    printf("I live in Bangladesh");
    return 0;
}


১. সি ল্যাঙ্গুয়েজ কমেন্ট (Comments)
কমেন্ট মানে হলো কোডের ভেতরে নিজের ভাষায় কোনো মন্তব্য বা নোট লিখে রাখা। কম্পিউটার বা কম্পাইলার যখন কোড রান করে, 
তখন সে কমেন্টগুলোকে সম্পূর্ণ উপেক্ষা (Ignore) করে চলে যায়। অর্থাৎ, কমেন্টের কারণে কোডের আউটপুটে কোনো প্রভাব পড়ে না।

কমেন্ট কেন ব্যবহার করা লাগে

নিজের লেখা কোড যেন কয়েক মাস পর দেখেও সহজে বোঝা যায়।

অন্য কোনো প্রোগ্রামার আপনার কোড দেখলে যেন সহজে বুঝতে পারেন কোন লাইনের কী কাজ।

কোড টেস্ট করার সময় কোনো লাইন সাময়িকভাবে বন্ধ করে রাখার জন্য।


ক) সিঙ্গেল-লাইন কমেন্ট (Single-line Comment)
যদি নোটটি মাত্র এক লাইনের হয়, তবে লাইনের শুরুতে দুটি ফরওয়ার্ড স্ল্যাশ (//) ব্যবহার করতে হয়।

// Single-line Comment:  //

int age = 22; // এখানে বয়স ডিফাইন করা হয়েছে (কম্পাইলার এই অংশটি পড়বে না)


খ) মাল্টি-লাইন কমেন্ট (Multi-line Comment)
যদি আপনার নোটটি বড় হয় এবং একাধিক লাইন জুড়ে লিখতে চান, তবে এটি ব্যবহার করতে হয়। এটি শুরু হয় /* দিয়ে এবং শেষ হয় */ দিয়ে।

Multi-line Comment: /*  */

/*
  প্রোগ্রামের নাম: যোগ করার কোড
  লেখক: আব্দুল্লাহ
  তারিখ: ২২ জুন, ২০২৬
*/

int sum = 10 + 20;





সি ল্যাঙ্গুয়েজ এস্কেপ সিকোয়েন্স (Escape Sequence)
যখন printf() ফাংশনের ভেতরে কোনো লেখা স্ক্রিনে দেখাই চাওয়া হয়, তখন মাঝে মাঝে আমাদের লাইন তৈরি করা, লেখার মাঝখানে স্পেস (Tab) দেওয়া, কিংবা কোটেশন চিহ্ন প্রিন্ট করার প্রয়োজন হয়। 
এই বিশেষ কাজগুলো করার জন্য ব্যাকস্ল্যাশ (\) এর সাথে একটি ক্যারেক্টার মিলিয়ে যে বিশেষ কোড তৈরি করা হয়, তাকেই Escape Sequence বলে।

এগুলোকে "এস্কেপ" বলার কারণ হলো, কম্পাইলার এদেরকে সাধারণ অক্ষর হিসেবে না দেখে এদের নিজস্ব অর্থ থেকে "এস্কেপ" করে বিশেষ কাজ সম্পন্ন করে।
 // use of \n
#include <stdio.h>
int main() {
    printf("Hello World!\n");
    printf("I am learning C.\n");
    printf("One\nTwo\nThree");

    return 0;
}


// use of \t
    printf("Name\tRoll\tSubject\n");
    printf("---------------------------\n");
    printf("Mohammad\t1\tMath\n");
    printf("Abdullah\t2\tScience\n");

    return 0;

// use of \"
#include <stdio.h>

int main() {
    printf("My favorite book is \"Al-Koran\".\n");
    printf("The teacher said, \"Be honest.\"");

    return 0;
}

// use of \\
#include <stdio.h>

int main() {
    printf("Backslash looks like this: \\ \n");
    printf("Folder path: D:\\MyFiles\\C_Programs /n");
    printf("The file location is: C:\\Windows\\System32");
    return 0;
}


#include <stdio.h>

int main() {
    // সিঙ্গেল-লাইন কমেন্ট: নিচে বিভিন্ন এস্কেপ সিকোয়েন্স টেস্ট করা হচ্ছে
    
    /* 
       মাল্টি-লাইন কমেন্ট:
       নিচের printf ফাংশনগুলোতে নিউলাইন (\n), ট্যাব (\t), 
       কোটেশন, ব্যাকস্ল্যাশ এবং অন্যান্য ফরম্যাটিং ট্রিকস ব্যবহার করা হয়েছে।
    */
    
    // ১. নিউলাইন (\n) এবং ট্যাব (\t) এর উদাহরণ
    printf("Learning programming is easy!\n"); 
    printf("Topic:\tComments\nSub-Topic:\tEscape Sequences\n");
    printf("-----------------------------------------\n");

    // ২. ডাবল কোটেশন (\") এবং সিঙ্গেল কোটেশন (\') এর উদাহরণ
    printf("Book Name:\t\"Computer Programming\"\n");
    printf("Author:\t\t\'Subeen\'\n");
    printf("-----------------------------------------\n");

    // ৩. ব্যাকস্ল্যাশ (\\) এর উদাহরণ
    printf("File Path:\tC:\\ProgramFiles\\C-Language\n");
    printf("-----------------------------------------\n");

    // ৪. ব্যাকস্পেস (\b) এর উদাহরণ
    // এটি কার্সারকে এক ঘর পেছনে নিয়ে যায় এবং 'x' কে 'd' দিয়ে রিপ্লেস (মুছে) করে দেয়
    printf("Wrong Text:\tAbcx\bd\n"); 
    printf("-----------------------------------------\n");

    // ৫. অ্যালার্ম / বিপ সাউন্ড (\a) এর উদাহরণ
    // এটি কম্পিউটারে একটি হালকা 'বিপ' শব্দ তৈরি করবে (যদি ওএস সাপোর্ট করে)
    printf("Alert Sound:\tPlaying Beep Now!\a\n");

    return 0;
}


\b আসলে কী করে?

কীবোর্ডের Backspace বাটন চাপলে যেমন আগের একটি অক্ষর মুছে যায়, সি ল্যাঙ্গুয়েজে \b ঠিক একই কাজ করে। এটি আউটপুট দেওয়ার সময় কার্সারকে এক ঘর পেছনে (Left) নিয়ে যায়। 
এরপর নতুন যে অক্ষরটি লিখা হবে, সেটি আগের অক্ষরের ওপর বসে যায় (Overwrite করে)।

#include <stdio.h>

int main() {
    // উদাহরণ ১: সাধারণ ব্যাকস্পেস
    // এখানে 'Abcx' লেখার পর কার্সার 'x' এর ওপর ফিরে আসবে এবং 'd' বসে যাবে।
    printf("Result 1: Abcx\bd\n"); 
    
    // উদাহরণ ২: একাধিক ব্যাকস্পেস ব্যবহার
    // এখানে '12345' লেখার পর দুটি \b\b দেওয়া হয়েছে, তাই কার্সার ২ ঘর পেছনে যাবে।
    // ফলে '45' এর জায়গায় '99' বসে যাবে।
    printf("Result 2: 12345\b\b99\n");

    return 0;
}

Result 1: Abd
Result 2: 12399

ব্যাখ্যা ১: Abcx প্রিন্ট হওয়ার পর \b এসে x কে টার্গেট করে এবং তার জায়গায় d বসিয়ে দেয়। তাই স্ক্রিনে দেখি Abd।

ব্যাখ্যা ২: 12345 প্রিন্ট হওয়ার পর পর পর দুটি \b\b এর কারণে 4 এবং 5 মুছে যায় (কার্সার পেছনে আসে) এবং সেখানে 99 বসে যায়। তাই আউটপুট আসে 12399।
