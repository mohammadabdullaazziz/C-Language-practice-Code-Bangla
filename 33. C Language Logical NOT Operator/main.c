সি (C) ল্যাঙ্গুয়েজে Logical NOT অপারেটর-কে ! (এক্সক্লামেশন বা ব্যাং) চিহ্ন দিয়ে প্রকাশ করা হয়。 এর কাজ হলো কোনো সত্য (True) শর্তকে মিথ্যা (False) করা এবং মিথ্যা (False) শর্তকে সত্য (True) করা। 
সহজ কথায়, এটি শর্তের ফলাফলকে উল্টে দেয়

সি ল্যাঙ্গুয়েজে (এমনকি জাভাস্ক্রিপ্টেও) Logical NOT Operator (!) একটি Unary Operator (ইউনারী অপারেটর)। 
"Unary" শব্দটা এসেছে "Unit" বা এক থেকে। যে সমস্ত অপারেটর শুধুমাত্র একটি মাত্র ভেরিয়েবল বা মানের (Operand) ওপর ভিত্তি করে নিজের কাজ সম্পাদন করতে পারে, সেগুলোকে Unary Operator বলা হয়।

যেহেতু Logical NOT (!) অপারেটরটি কাজ করার জন্য তার ডানপাশে কেবল একটি মাত্র মান বা কন্ডিশন খোঁজে, তাই এটি একটি বিশুদ্ধ ইউনারী অপারেটর।

সত্যের উল্টো: যদি কোনো শর্ত বা মানের ফলাফল True (যেকোনো নন-জিরো মান, যেমন 1) হয়, তবে তার আগে ! বসালে তা False বা 0 হয়ে যায়।
মিথ্যার উল্টো: যদি কোনো শর্তের ফলাফল False (0) হয়, তবে তার আগে ! বসালে তা True বা 1 হয়ে যায়।

0 মানে হলো মিথ্যা (False)।0 ব্যতীত অন্য যেকোনো সংখ্যা (যেমন 1, 5, -2) মানে হলো সত্য (True)।
0 (শূন্য): সি ল্যাঙ্গুয়েজে শুধুমাত্র 0 কে False ধরা হয়। যে কোনো সংখ্যা (১, ৫, -১০ বা ১০০): শূন্য ছাড়া পৃথিবীর সকল পজিটিভ ও নেগেটিভ সংখ্যাকে সি ল্যাঙ্গুয়েজ True মনে করে।

সি ল্যাঙ্গুয়েজে (এবং জাভাস্ক্রিপ্টেও) Logical NOT (!) অপারেটর তার ডানপাশের অপারেন্ড বা মানটিকে প্রথমে মনে মনে একটি বুলিয়ান (Boolean) মানে রূপান্তর করে, এবং তারপর সেটাকে উল্টে দেয়।

!True ------- False (অর্থাৎ কোনো সংখ্যার সামনে ! দিলে উত্তর আসবে 0)
!False -----  Core True (অর্থাৎ 0 এর সামনে ! দিলে উত্তর আসবে 1)


সংখ্যা শূন্য (০) নাকি অন্য কিছু তা চেক করা
আমরা জানি, সি ল্যাঙ্গুয়েজে 0 মানে False আর অন্য যেকোনো সংখ্যা মানে True।

#include <stdio.h>

int main() {
    int number = 0;

    // !number মানে হলো— number যদি শূন্য (False) হয়, তবে সেটাকে উল্টে True (১) বানাবে
    if (!number) {
        printf("সংখ্যাটি হলো শূন্য (০)।\n");
    } else {
        printf("সংখ্যাটি শূন্য নয়, এটি একটি ভ্যালিড সংখ্যা।\n");
    }

    return 0;
}

আউটপুট: সংখ্যাটি হলো শূন্য (০)।

ব্যাখ্যা: number = 0 (False)। !0 হয়ে গেল True (১), তাই if ব্লকের ভেতরের কোডটি রান করলো।


বয়স চেক করা (কন্ডিশন উল্টে দেওয়া) 
একটি স্পেশাল রাইডে চড়তে হলে বয়স ১৮ এর নিচে হওয়া যাবে না।

#include <stdio.h>

int main() {
    int age = 15;

    // (age >= 18) এর উত্তর আসবে False (০), কারণ ১৫ কিন্তু ১৮ থেকে বড় নয়।
    // এবার বাইরের ! চিহ্ন ওই False কে উল্টে True (১) বানিয়ে দেবে।
    if (!(age >= 18)) {
        printf("দুঃখিত, আপনার বয়স ১৮ এর কম! আপনি এই রাইডে চড়তে পারবেন না।\n");
    } else {
        printf("অভিনন্দন! আপনি রাইডে চড়তে পারবেন।\n");
    }

    return 0;
}
আউটপুট: দুঃখিত, আপনার বয়স ১৮ এর কম! আপনি এই রাইডে চড়তে পারবেন না।

    

বুলিয়ান ফ্ল্যাগ (Boolean Flag) এর বাস্তব ব্যবহার
বাস্তব জীবনের প্রজেক্টে কোনো কিছুর অন/অফ বা হা/না স্টেট ট্র্যাক করতে এটি সবচেয়ে বেশি ব্যবহৃত হয়। (এর জন্য <stdbool.h> ব্যবহার)।

#include <stdio.h>
#include <stdbool.h>

int main() {
    bool isAccountActive = false; // অ্যাকাউন্টটি বর্তমানে অ্যাক্টিভ নেই (False)

    // !isAccountActive মানে হলো— অ্যাকাউন্ট যদি অ্যাক্টিভ না হয় (Not Active)
    if (!isAccountActive) {
        printf("আপনার অ্যাকাউন্টটি বন্ধ আছে। দয়া করে ভেরিফাই করুন।\n");
    } else {
        printf("স্বাগতম! আপনার অ্যাকাউন্টটি সচল আছে।\n");
    }

    return 0;
}
আউটপুট: আপনার অ্যাকাউন্টটি বন্ধ আছে। দয়া করে ভেরিফাই করুন।



সরাসরি মান প্রিন্ট করে দেখা (printf দিয়ে ড্রাই-রান)
কন্ডিশন ছাড়া সরাসরি printf দিয়ে:

#include <stdio.h>

int main() {
    int x = 100;
    int y = 0;

    printf("!x এর মান: %d\n", !x); // ১০০ (True) কে উল্টে দেবে
    printf("!y এর মান: %d\n", !y); // ০ (False) কে উল্টে দেবে

    return 0;
}
!x এর মান: 0
!y এর মান: 1


পাসওয়ার্ড বা ইনপুট বক্স ফাঁকা (Empty) কিনা চেক করা
বাস্তবে যখন কোনো ইউজার সাইন-আপ করার সময় পাসওয়ার্ড না দিয়েই "সাবমিট" বাটনে ক্লিক করে, তখন ব্যাকএন্ড বা সিস্টেমে এই লজিকটা কাজ করে।

#include <stdio.h>
#include <stdbool.h>

int main() {
    bool isPasswordEntered = false; // ইউজার পাসওয়ার্ড দেয়নি (False)

    // !isPasswordEntered মানে হলো— "যদি পাসওয়ার্ড দেওয়া না হয়ে থাকে"
    if (!isPasswordEntered) {
        printf("ত্রুটি (Error): পাসওয়ার্ডের ঘরটি ফাঁকা রাখা যাবে না!\n");
    } else {
        printf("পাসওয়ার্ড সফলভাবে নেওয়া হয়েছে।\n");
    }

    return 0;
}
আউটপুট: ত্রুটি (Error): পাসওয়ার্ডের ঘরটি ফাঁকা রাখা যাবে না!

    

লুপ কন্ট্রোল করা (Toggle Switch)
লজিক্যাল NOT এর অন্যতম সেরা ব্যবহার হলো কোনো জিনিসের স্টেটকে এক চাপেই বদলে দেওয়া (যেমন ফ্যানের সুইচে একবার চাপ দিলে অন হয়, আরেকবার দিলে অফ হয়)। একে কোডিংয়ের ভাষায় Toggle করা বলে।

#include <stdio.h>
#include <stdbool.h>

int main() {
    bool lightOn = false; // শুরুতে লাইট বন্ধ (False)

    printf("১ম বার সুইচে চাপ দেওয়ার আগে লাইট: %d\n", lightOn);

    // সুইচে চাপ দেওয়া হলো: লাইটের স্টেট যা ছিল তার উল্টোটা হয়ে যাবে
    lightOn = !lightOn; 
    printf("১ম বার সুইচে চাপ দেওয়ার পর লাইট: %d\n", lightOn);

    // আবার সুইচে চাপ দেওয়া হলো
    lightOn = !lightOn; 
    printf("২য় বার সুইচে চাপ দেওয়ার পর লাইট: %d\n", lightOn);

    return 0;
}
১ম বার সুইচে চাপ দেওয়ার আগে লাইট: 0
১ম বার সুইচে চাপ দেওয়ার পর লাইট: 1
২য় বার সুইচে চাপ দেওয়ার পর লাইট: 0

    

লিপ ইয়ার (Leap Year) লজিক সহজ করা
লিপ ইয়ার বের করার একটা বড় শর্ত হলো—বছরটিকে ৪ দিয়ে ভাগ করা যাবে কিন্তু ১০০ দিয়ে ভাগ করা যাবে না। NOT অপারেটর দিয়ে এই লজিকটা খুব সহজে এক লাইনে লেখা যায়।

#include <stdio.h>

int main() {
    int year = 2026;

    // (year % 100 == 0) এর মানে ১০০ দিয়ে ভাগ যায়।
    // সামনে ! দেওয়াতে এর মানে দাঁড়ালো— "১০০ দিয়ে ভাগ যাওয়া যাবে না"
    if ((year % 4 == 0) && !(year % 100 == 0)) {
        printf("%d সালটি একটি লিপ ইয়ার।\n", year);
    } else {
        printf("%d সালটি লিপ ইয়ার নয়।\n", year);
    }

    return 0;
}
আউটপুট: 2026 সালটি লিপ ইয়ার নয়।

ব্যাখ্যা: ২০২৬ সালকে ১০০ দিয়ে ভাগ করা যায় না, তাই ব্র্যাকেটের ভেতর উত্তর আসে False (০)। কিন্তু বাইরে ! থাকায় সেটা হয়ে যায় True (১)। তবে যেহেতু প্রথম শর্তটা (২০২৬ % ৪ == ০) মিথ্যা, 
তাই পুরো কন্ডিশনটাই মিথ্যা হয়ে else ব্লক রান করেছে।



সীমার বাইরে (Out of Range) সংখ্যা চেক করা
১ থেকে ১০ এর মধ্যে যেকোনো একটা সংখ্যা ইনপুট । যদি এর বাইরের কোনো সংখ্যা দেয় (যেমন ১১ বা ০), তবে সিস্টেম এরর দেখাবে।
    
#include <stdio.h>

int main() {
    int inputNumber = 15;

    // (inputNumber >= 1 && inputNumber <= 10) এর মানে হলো সংখ্যাটি ১ থেকে ১০ এর ভেতরে আছে।
    // সামনে ! দেওয়াতে পুরোটার মানে দাঁড়ালো— "যদি সংখ্যাটি ১ থেকে ১০ এর ভেতরে না থাকে"
    if (!(inputNumber >= 1 && inputNumber <= 10)) {
        printf("ভুল ইনপুট! দয়া করে ১ থেকে ১০ এর মধ্যে সংখ্যা দিন।\n");
    } else {
        printf("ধন্যবাদ! সঠিক সংখ্যা দিয়েছেন।\n");
    }

    return 0;
}

আউটপুট: ভুল ইনপুট! দয়া করে ১ থেকে ১০ এর মধ্যে সংখ্যা দিন।

ভেতরের লজিক: ১৫ যেহেতু ১ থেকে ১০ এর ভেতর নেই, তাই ব্র্যাকেটের ভেতরের অংশটা হয়েছিল False (0)। বাইরের ! সেটাকে উল্টে True (1) করে দিল, আর ইফ ব্লকটা রান হয়ে গেল।



"গেম পজ" (Game Pause) মেকানিজম
লুডু, ফ্রি ফায়ার বা যেকোনো গেম খেলার সময় যখন "Pause" বাটনে চাপ দিই, তখন গেমটা থেমে যায়। আবার চাপ দিলে চলা শুরু করে। হোয়াইল লুপের (while loop) ভেতর এই লজিকটা কীভাবে কাজ করে

#include <stdio.h>
#include <stdbool.h>
int main() {
    bool isPaused = false; // শুরুতে গেম পজ করা নেই, অর্থাৎ গেম চলছে
    int score = 0;

    // !isPaused মানে হলো— "যতক্ষণ গেমটি পজ করা থাকবে না"
    while (!isPaused) {
        score += 10;
        printf("গেম চলছে... আপনার বর্তমান স্কোর: %d\n", score);

        // একটা শর্ত দিয়ে গেমটা মাঝপথে পজ করে দিই
        if (score == 30) {
            isPaused = true; // গেম পজ করে দেওয়া হলো
        }
    }

    printf("গেমটি পজ করা হয়েছে।\n");
    return 0;
}
গেম চলছে... আপনার বর্তমান স্কোর: 10
গেম চলছে... আপনার বর্তমান স্কোর: 20
গেম চলছে... আপনার বর্তমান স্কোর: 30
গেমটি পজ করা হয়েছে।




ক্যারেক্টার বা অক্ষর চেক করা (Not Equal এর বিকল্প)
সি ল্যাঙ্গুয়েজে সরাসরি != (Not Equal) ব্যবহার করতে পারি, তবে লজিক্যাল NOT দিয়েও একই কাজ অন্যভাবে করা যায়। যেমন—ইউজার Yes চাপেনি, এটা চেক করা:

#include <stdio.h>
int main() {
    char ch = 'n';

    // (ch == 'y') মানে চয়েস কি 'y'? উত্তর: False (0)
    // !(ch == 'y') মানে চয়েস কি 'y' নয়? উত্তর: True (1)
    if (!(ch == 'y')) {
        printf("ইউজার 'y' চাপেনি, অন্য কিছু চেপেছে।\n");
    } else {
        printf("ইউজার 'y' চেপেছে।\n");
    }

    return 0;
}
আউটপুট: ইউজার 'y' চাপেনি, অন্য কিছু চেপেছে।


🔹 উদাহরণ ১: স্টুডেন্ট প্রোফাইল ও সিজিপিএ (CGPA) ভ্যালিডেশন
 সিস্টেমে একজন শিক্ষার্থীর তথ্য আছে। তার বয়স এবং সিজিপিএ দুটোই ঠিকঠাক (Valid) আছে কিনা, তা এক লাইনে সব অপারেটর দিয়ে চেক।


#include <stdio.h>
#include <stdbool.h>

int main() {
    // ১. Assignment Operator দিয়ে মান বসানো হলো
    int age = 30;
    float cgpa = 3.85;

    // ২. Relational, Logical AND এবং NOT অপারেটরের মিক্সড লজিক
    // এখানে চেক হচ্ছে: বয়স যদি ০ বা তার কম হয়, অথবা সিজিপিএ যদি ৪.০০ এর বেশি হয়
    if (!(age > 0 && cgpa <= 4.00)) {
        printf("সিস্টেম এরর: বয়স বা সিজিপিএ এর মান ভুল দেওয়া হয়েছে!\n");
    } else {
        printf("প্রোফাইল ভ্যালিড! বয়স: %d, সিজিপিএ: %.2f\n", age, cgpa);
    }

    return 0;
}

ব্যবহৃত অপারেটর: =, >, <=, &&, !
আউটপুট: প্রোফাইল ভ্যালিড! বয়স: 30, সিজিপিএ: 3.85ভেতরের খেলা: ব্র্যাকেটের ভেতরের দুটি শর্তই সত্য (True && True ---- True)। কিন্তু বাইরে একটা ! থাকায় পুরোটা উল্টে হয়ে গেল False (0)। তাই else ব্লকটি রান হলো।


উদাহরণ ২: একটি সংখ্যার বর্গ (Square) বের করে তা জোড় নাকি বিজোড় চেক করা
একই সাথে অ্যারিথমেটিক (গুণ) এবং কন্ডিশনাল লজিক একসাথে ব্যবহার।

#include <stdio.h>
int main() {
    int number = 5;
    
    // ১. Arithmetic (*) এবং Assignment (=) অপারেটর
    int square = number * number; // ৫ * ৫ = ২৫

    printf("সংখ্যাটির বর্গ হলো: %d\n", square);

    // ২. Relational (==) অপারেটর এবং লজিক্যাল NOT (!)
    // (square % 2 == 0) এর মানে হলো সংখ্যাটি জোড়। 
    // সামনে ! দেওয়াতে এর মানে দাঁড়ালো— "যদি সংখ্যাটি জোড় না হয় (অর্থাৎ বিজোড়)"
    if (!(square % 2 == 0)) {
        printf("বর্গ সংখ্যাটি একটি বিজোড় সংখ্যা।\n");
    } else {
        printf("বর্গ সংখ্যাটি একটি জোড় সংখ্যা।\n");
    }

    return 0;
}
ব্যবহৃত অপারেটর: *, =, %, ==, !
আউটপুট: ```text
সংখ্যাটির বর্গ হলো: 25
বর্গ সংখ্যাটি একটি বিজোড় সংখ্যা।



Arithmetic Operator: * (গুণ), % (ভাগশেষ)

Assignment Operator: = (মান বসানো)

Relational Operator: == (সমান কিনা চেক করা), && (লজিক্যাল অ্যান্ড)

Logical NOT Operator: ! (উল্টে দেওয়া)

এই সবগুলো অপারেটর একসাথে মিলিয়ে সি ল্যাঙ্গুয়েজের একটি বাস্তব উদাহরণ নিচে দেওয়া হলো।  যেখানে ইউজারের আইডি নম্বর এবং সিকিউরিটি পিন (PIN) ভেরিফাই করে তাকে সিস্টেমে লগইন করতে দেওয়া হবে।

#include <stdio.h>
#include <stdbool.h>

int main() {
    // ১. Assignment Operator (=) দিয়ে ডেটা সেট করা হলো
    int userId = 1001;
    int enteredPin = 1234;
    int correctPin = 1234;
    bool isAccountLocked = false; // অ্যাকাউন্ট লক করা নেই (False)

    // ২. মিক্সড লজিক্যাল এবং রিলেশনাল অপারেটরের খেলা
    // প্রথম অংশ: (enteredPin == correctPin) -> পিন মিলেছে কিনা (True)
    // দ্বিতীয় অংশ: (!isAccountLocked) -> অ্যাকাউন্ট কি লকড "নয়" (True)
    if ((enteredPin == correctPin) && !isAccountLocked) {
        
    printf("অভিনন্দন! ইউজার %d এর লগইন সফল হয়েছে।\n", userId);
        
    // ৩. Arithmetic (*) এবং Assignment (=) অপারেটরের ব্যবহার
    int bonusPoints = 5;
    int finalScore = bonusPoints * 10; // গুণ করা হলো (৫০)
    printf("আপনার লগইন বোনাস স্কোর: %d\n", finalScore);

    } else {
        printf("লগইন ব্যর্থ! পিন ভুল অথবা অ্যাকাউন্টটি লক করা আছে।\n");
    }

    return 0;
}
অভিনন্দন! ইউজার 1001 এর লগইন সফল হয়েছে。
আপনার লগইন বোনাস স্কোর: 50


🔍 কোডের ভেতরের লজিক ব্রেক-ডাউন:
    
কম্পিউটার এই বড় if কন্ডিশনটা কীভাবে এক লাইনে হিসাব করলো দেখুন:

প্রথমে সে দেখলো enteredPin == correctPin। যেহেতু ১২৩৪ আর ১২৩৪ মিলে গেছে, তাই এর মান হলো True (১)।

এরপর সে দেখলো !isAccountLocked। যেহেতু ভেরিয়েবলের মান ছিল false (০), তাই লজিক্যাল NOT (!) সেটাকে উল্টে বানিয়ে দিল True (১)।

সবশেষে মাঝখানের লজিক্যাল অ্যান্ড (&&) হিসাব করলো: True && True। আমরা জানি অ্যান্ড অপারেটরের দুই পাশেই ট্রু থাকলে ফাইনাল উত্তর হয় True (১)।

যেহেতু পুরো কন্ডিশনটা সত্য হলো, তাই কম্পিউটার সফলভাবে ভেতরের কোডটুকু রান করে আউটপুট দেখিয়ে দিল।
    



#include <stdio.h>
int main() {
    printf("শুধুমাত্র 5 এর মান: %d\n", 5);
    printf("একটা নট দিয়ে !5 এর মান: %d\n", !5);
    printf("দুইটা নট দিয়ে !!5 এর মান: %d\n", !!5);

    return 0;
}
কম্পিউটার আসলে একবারে দুটো চিহ্ন দেখতে পারে না, সে সবসময় ডান দিক থেকে বাম দিকে (Right to Left) একটা একটা করে ব্র্যাকেটের মতো ভেঙে কাজ করে।
    
🕵️‍♂️ ধাপ ১: মূল ভ্যালুটা কী? (The Operand)
আসল সংখ্যাটি হলো 5।
সি ল্যাঙ্গুয়েজের সেই গোল্ডেন রুলটা মনে করুন—শূন্য (0) ছাড়া পৃথিবীর যেকোনো সংখ্যাই হলো মনে মনে True।
তার মানে, কম্পিউটার শুরুতেই ৫-কে ধরে নিল: True।

🔄 ধাপ ২: প্রথম ! এর আক্রমণ (ডান পাশের NOT)
কম্পিউটার এবার ৫ এর ঠিক পাশে থাকা প্রথম আশ্চর্যবোধক চিহ্নটির কাজ করবে।

কোডটা তখন দেখতে এমন: !( !5 )

ভেতরের অংশটা হিসাব হবে: !True

লজিক্যাল NOT এর কাজই হলো উল্টে দেওয়া। তাই সে True-কে উল্টে বানিয়ে দিল False (অর্থাৎ সি ল্যাঙ্গুয়েজের ভাষায় 0)।
হাতে রইল শুধু বাইরের বাম পাশের ! চিহ্নটি এবং ভেতরের নতুন মান 0।

🔄 ধাপ ৩: দ্বিতীয় ! এর আক্রমণ (বাম পাশের NOT)
এবার ফাইনাল রাউন্ড। বাইরের শেষ ! চিহ্নটি ভেতরের ওই 0 বা False-কে আক্রমণ করবে।

কোডটা এখন দেখতে এমন: !(0) অথবা !(False)

লজিক্যাল NOT আবারও তার স্বভাবসুলভ কাজ করবে—সে এবার এই False বা 0-কে উল্টে দেবে।

False-কে উল্টে দিলে কী হয়? True। আর সি ল্যাঙ্গুয়েজে পিওর True-এর মান কত? 1।

সহজ কথায়, প্রথম নট (!) ভ্যালুটাকে উল্টে দেয়, আর দ্বিতীয় নট (!) সেটাকে আবার উল্টে আগের জায়গায় নিয়ে আসে।

💡 এই ডাবল নট (!!) এর আসল লাভটা
কোনো সাধারণ সংখ্যা বা ডেটাকে (যেমন: ৫, ১০, বা কোনো নাম) তার আসল মানের কোনো পরিবর্তন না করে, 
শুধুমাত্র বিশুদ্ধ বুলিয়ান (১ বা ০ / True বা False) এ কনভার্ট করতে চাই, তখন এই ডাবল নট ট্রিকটা ব্যবহার করা হয়। এটি কোডকে অনেক ফাস্ট এবং মেমরি সেভিং করে।



🧠 ব্রেন টিজার: ট্রিপল নট ( !!! ) দিলে কী হবে? !!!5, তবে আউটপুট কী আসবে?

ধাপ ১: 5 (True)ধাপ
২: প্রথম নট এসে করলো !True ----- False (0)ধাপ 
৩: দ্বিতীয় নট এসে ওই ফলসকে আবার উল্টালো !False -------- True (1)ধাপ 
৪: এবার শেষ বা তৃতীয় নট এসে ওই ট্রু-কে আবার উল্টে দেবে !True ------ False (0)!

তার মানে হলো: জোড় সংখ্যক (২, ৪, ৬টি) নট দিলে আসল বুুলিয়ান মান ফেরত আসে, আর বিজোড় সংখ্যক (১, ৩, ৫টি) নট দিলে মানটা উল্টে যায়।
