Logical OR Operators:

C-তে Logical OR (||) অপারেটর একাধিক কন্ডিশনের মধ্যে অন্তত একটি সত্য (True) হলে ফলাফল সত্য বা ১ (True) প্রদান করে। 
এটি একটি বাইনারি অপারেটর, যা দুটি শর্তের যেকোনো একটি পূর্ণ হলেই পুরো শর্তটিকে সত্য বলে গণ্য করে।
শুধুমাত্র সব শর্ত মিথ্যা (False) হলে এটি মিথ্যা বা ০ প্রদান করে।

C ল্যাঙ্গুয়েজ বা প্রোগ্রামিংয়ের ভাষায় || কে বলা হয় Logical OR Operator।

সাধারণ কিবোর্ড বা সাধারণ ভাষায় একে  Double Pipe (ডাবল পাইপ) চিহ্নও বলা হয়।

Logical OR তখনই False (0) রিটার্ন করে যখন তার দুই পাশের সবকটি কন্ডিশনই মিথ্যা হয়। অন্যথায়, যেকোনো এক পাশ সত্য হলেই ফলাফল True (1) হয়।

False (0) || False (0)	0 (False)
False (0)	|| True (1)	1 (True)
True (1)	|| False (0)	1 (True)
True (1)	|| True (1)	1 (True)

(Logical OR) অপারেটর বাম থেকে ডানে (Left to Right) কাজ করে। একে প্রোগ্রামিংয়ের ভাষায় Left-to-Right Associativity বলা হয়।

যখন কম্পাইলার (Condition1 || Condition2 || Condition3) দেখে, সে প্রথমে সবার বামের Condition1 চেক করে।

যদি Condition1 সত্য (True) হয়, তবে সে আর ডানের বাকি কন্ডিশনগুলো (Condition2 বা Condition3) চেকই করে না। সরাসরি পুরো ফলাফলকে True বলে ধরে নেয়।

যদি Condition1 মিথ্যা (False) হয়, কেবল তখনই সে ডানের Condition2 চেক করতে যায়।


সি ল্যাঙ্গুয়েজে মূলত 0 (শূন্য) কেই একমাত্র False ধরা হয়। এছাড়া বাকি সব কিছুকে True ধরা হয়।

0 (Integer zero): সাধারণ পূর্ণ সংখ্যা 0 হলো সরাসরি False।

0.0 (Floating point zero): দশমিকের ক্ষেত্রেও 0.0 কে False ধরা হয়।

'\0' (Null Character): স্ট্রিং বা ক্যারেক্টারের শেষে যে নাল ক্যারেক্টার থাকে, তার মানও শূন্য, তাই এটিও False।

যদি প্রোগ্রামের শুরুতে #include <stdbool.h> ব্যবহার করা হয় , তবে সরাসরি false কিউয়োর্ডটি ব্যবহার করা যাবে। এটি কম্পাইলারের কাছে ইন্টারনালি 0 হিসেবেই গণ্য হয়।

False: শুধুমাত্র 0। || (OR) অপারেটর ব্যবহার করলে, তখন সেটি এই 0 বা non-zero মানগুলো চেক করে।

True: 0 ছাড়া অন্য যেকোনো সংখ্যাই True। সেটা 1 হতে পারে, 100 হতে পারে, এমনকি নেগেটিভ সংখ্যা যেমন -5 হলেও সেটি True হিসেবে কাজ করবে।



C ল্যাঙ্গুয়েজে || অপারেটর যখন কাজ করে, তখন সে সবশেষে পুরো লজিকের রেজাল্ট হিসেবে ১ (True) অথবা ০ (False) রিটার্ন করে।
  
যদি সে কোনো একটি True ভ্যালু খুঁজে পায়, তবে সে সেখানেই থমকে যায় এবং পুরো এক্সপ্রেশনের মান হিসেবে ১ রিটার্ন করে।

যদি সে শেষ পর্যন্ত কোনো True না পায় (সবগুলোই ০ বা False হয়), তবেই সে ০ রিটার্ন করে।

#include <stdio.h>
#include <stdbool.h> 
int main(){
    int a = 5;
    int b = 0;
    char name[] = "Abdullah";

    // (a || b || name) এর মান কত তা দেখাবে
    printf("Logical Result: %d\n", (a || b || name));
    return 0;
}

প্রথমে সে বাম দিক থেকে a (মান ৫) চেক করবে।

যেহেতু ৫ একটি Non-zero সংখ্যা, তাই এটি True।

প্রথমেই True পেয়ে গেছে, তাই সে আর ডানের b বা name চেক করতে যাবে না।

সে সরাসরি পুরো লজিকের মান হিসেবে ১ (True) রিটার্ন করবে।

যখন (a || b || name), তখন এটি আর ৩টি আলাদা ভ্যালু থাকে না। এটি একটি লজিক্যাল এক্সপ্রেশন বা একটি গাণিতিক সমস্যার মতো হয়ে যায়।

কম্পাইলার যখন ব্র্যাকেটের ভেতরের অংশটি সমাধান (Solve) করে, তখন সে সবশেষে একটি মাত্র রেজাল্ট বের করে। 
যেহেতু লজিক্যাল OR (||) সবসময় সবশেষে একটি মাত্র সংখ্যা (১ অথবা ০) দেয়, তাই ওই একটি মাত্র সংখ্যা প্রিন্ট করার জন্য  একটিই %d প্রয়োজন হয়।

printf("%d, %d, %s", a, b, name); তাহলে ৩টি ফরম্যাট স্পেসিফায়ার দিতে হতো, কারণ এখানে ৩টি আলাদা আলাদা ভ্যালু দেখতে চাওয়া হসছে।
এটি একটি OR (||) অপারেশন, একটি সত্য পাওয়া মানেই পুরো ব্র্যাকেটের সমাধান হয়ে যাওয়া। পুরো ব্র্যাকেটটি মুছে গিয়ে সেখানে শুধু 1 বসে যায়। ফাংশনটি হয়ে যায়: printf("Logical Result: %d\n", 1);
শেষ পর্যন্ত হাতে শুধু একটি সংখ্যা (1) আছে, তাই একটিই %d যথেষ্ট।



#include <stdio.h>
int main() {
    int a = 0;
    int b = 0;
    int c = 0;

    // সব কন্ডিশন চেক করবে, কোথাও True পাবে না
    int result = (a || b || c);

    printf("Result: %d\n", result); // আউটপুট আসবে 0
    return 0;
}
C ল্যাঙ্গুয়েজে || (OR) অপারেটর যখন সব কন্ডিশন False পায়, তখন সে আসলে শেষের ভ্যালুটি রিটার্ন করে না, বরং সে ০ (False) রিটার্ন করে। C ল্যাঙ্গুয়েজে এটি সবসময় ০ অথবা ১ রিটার্ন করবে। এবং
সে যেখানেই True পাক না কেন, সে আসল ভ্যালুটি রিটার্ন না করে সবসময় 1 রিটার্ন করবে।

#include <stdio.h>
int main(){
  // ০.০ ও কিন্তু এক ধরনের False
  if (0 || 0.0 || 0)
  {
    printf("True");
  }
  else
  {
    printf("False (Result is 0)");
  }
  return 0;
}

সি ল্যাঙ্গুয়েজে যাই ইনপুট দেওয়া হক না কেন, সে সেটাকে প্রসেস করে সবশেষে একটি Integer (০ অথবা ১)

// C Language Example
#include <stdio.h>
int main() {
    int a = 0;
    int b = 0;
    int c = 0;

    int result = (a || b || c); 

    printf("%d", result); // আউটপুট আসবে: 0
    return 0;
}


#include <stdio.h>
int main(){
  int a = 0;
  int b = 5;
  int c = 2;
  int value = 0 || (a > b) || c;
  printf("value is: %d\n", value);
  return 0;
}

এখানে c = 2। আমরা জানি C ল্যাঙ্গুয়েজে শূন্য (0) ছাড়া যেকোনো সংখ্যাই True। যেহেতু 2 একটি Non-zero সংখ্যা, তাই কম্পাইলার একে True হিসেবে গণ্য করবে।
সে একটি True ভ্যালু খুঁজে পেয়েছে, তাই পুরো value ভেরিয়েবলের মান হয়ে যাবে ১ (True)।

C ল্যাঙ্গুয়েজে || অপারেটর মূলত Integer (পূর্ণসংখ্যা) রিটার্ন করে, যা লজিক্যালি বুলিয়ানের মতো কাজ করে।
C ল্যাঙ্গুয়েজে লজিক্যাল অপারেটরগুলোর (||, &&, !) কাজই হলো আপনাকে একটি বুলিয়ান উত্তর দেওয়া। সি-র কাছে "বুলিয়ান উত্তর" মানে হলো হয় ১ (সত্য) অথবা ০ (মিথ্যা)।
  
#include <stdio.h>
int main() {
    int a = 0;
    int b = 500; // একটি বড় সংখ্যা (True)
    int c = 20;  // এটিও একটি সংখ্যা (True)

    int result = (a || b || c); 

    printf("Result is: %d\n", result); 
    return 0;
}

কেন বলা হয় "Integer রিটার্ন করে"? (প্রযুক্তিগত দিক)

C ল্যাঙ্গুয়েজে int বা পূর্ণসংখ্যা হলো একটি মৌলিক ডেটা টাইপ। যখন 5 || 0, সি ল্যাঙ্গুয়েজ মেমোরিতে এই রেজাল্টটি রাখার জন্য একটি Integer জায়গা ব্যবহার করে।

সি-তে লজিক্যাল অপারেশনের রেজাল্ট আসলে int টাইপের ডাটা।

এ কারণেই  printf("%d", 5 || 0); ব্যবহার করলে, যেখানে %d ইনটিজারের ফরম্যাট স্পেসিফায়ার।

কেন বলা হয় "বুলিয়ান উত্তর দেয়"? (লজিক্যাল দিক)

এটি মূলত লজিক বোঝানোর জন্য বলা হয়। প্রোগ্রামিংয়ের ভাষায় "বুলিয়ান" মানেই হলো সত্য (True) বা মিথ্যা (False)।
  
সি ল্যাঙ্গুয়েজে আলাদাভাবে true বা false বলতে কিছু নেই (যদি না stdbool.h ব্যবহার করা না হয়)।

তাই সি ল্যাঙ্গুয়েজ ১ কে সত্য এবং ০ কে মিথ্যা হিসেবে ধরে নিয়ে একটি "বুলিয়ান সিদ্ধান্ত" দেয়।


#include <stdio.h>
#include <stdbool.h>
int main(){
  char name[] = "Abdullah";
  int a = 5;
  int b = 0;
  // (a || b || name) এর মান কত তা দেখাবে
  printf("Logical Result: %d\n", (name || a || b));
  return 0;
}

সি ল্যাঙ্গুয়েজে লজিক্যাল অপারেটর (||, &&) যখন কাজ করে, তখন তারা ইনপুট হিসেবে যাই পাক না কেন (সংখ্যা বা স্ট্রিং), 
আউটপুট হিসেবে সবসময় ১ (True) অথবা ০ (False) দেয়। যেহেতু এখানে একটি শর্ত সত্য হয়েছে, তাই সে ১ রিটার্ন করেছে।

সি ল্যাঙ্গুয়েজে একটি স্ট্রিং বা ক্যারেক্টার অ্যারে মূলত একটি মেমোরি অ্যাড্রেস। যেহেতু এই অ্যাড্রেসটি শূন্য (NULL) নয়, তাই কম্পাইলার একে True হিসেবে ধরে নেয়।
যেহেতু সে প্রথম শর্তেই True পেয়ে গেছে, তাই সে আর ডানের a বা b চেক করতে যাবে না।
