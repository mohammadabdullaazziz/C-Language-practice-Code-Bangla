C ল্যাঙ্গুয়েজে কমা অপারেটরকে বলা হয় "সবচেয়ে কম ক্ষমতাশালী" (Lowest Precedence) অপারেটর।
এর কাজ হলো একাধিক এক্সপ্রেশনকে (Expression) এক লাইনে বেঁধে ফেলা এবং বাম থেকে ডানে (Left-to-Right) একে একে সবগুলোর কাজ শেষ করা।
C ল্যাঙ্গুয়েজে কমা অপারেটর বাঁ দিক থেকে কাজ করা শুরু করলেও সবার শেষে বা একদম ডানে যে মানটি থাকে, সেটিই পুরো লাইনের রেজাল্ট হিসেবে রিটার্ন করে।

#include <stdio.h>
int main(){
    int x = (5, 10, 15);
    printf("Result is: %d\n", x);
    return 0;
}

#include <stdio.h>
int main(){
    int num = (5 + 2, 10 + 15);
    printf("Result is: %d\n", num);
    return 0;
}

#include <stdio.h>
int main(){
   int amount;
   int amount2;
   int result = (amount = 1, amount2 = 5, amount + amount2);
   printf("This Result: %d\n", result);
   return 0;
}

কেন ব্র্যাকেট () দেওয়া জরুরি?:
কমা অপারেটরের ক্ষমতা সবচেয়ে কম হওয়ায় ব্র্যাকেট না দিয়ে লিখলে:
int x = 5, 10, 15;

তাহলে কম্পাইলার মনে করত int x = 5 ডিক্লেয়ার করা হয়েছে এবং বাকি 10 ও 15 দেখে সে এরর (Error) দিয়ে দিত। তাই মান অ্যাসাইন করার সময় অবশ্যই প্রথম বন্ধনী () ব্যবহার করতে হয়। 
#include <stdio.h>

int main() {
    int x, y;

    // ১ নম্বর কেস (ব্র্যাকেট সহ)
    x = (5, 10, 15);

    // ২ নম্বর কেস (ব্র্যাকেট ছাড়া)
    y = 5, 10, 15;

    printf("x er man: %d\n", x);
    printf("y er man: %d\n", y);

    return 0;
}

প্রফেশনাল কোডিংয়ে কমা অপারেটরের সবচেয়ে বেশি ব্যবহার হয় for লুপের ভেতরে। 
যখন একই সাথে দুটি ভেরিয়েবল নিয়ে লুপ চালাতে হয়, তখন কমা অপারেটর ছাড়া আর কোনো উপায় থাকে না।
#include <stdio.h>

int main() {
    int i, j;

    // i বাড়বে ১ করে, আর j কমবে ১০ করে। মাঝখানে কমা অপারেটর ব্যবহার করা হয়েছে।
    for (i = 1, j = 100; i <= 5; i++, j = j - 10) {
        printf("i: %d, j: %d\n", i, j);
    }

    return 0;
}

i: 1, j: 100
i: 2, j: 90
i: 3, j: 80
i: 4, j: 70
i: 5, j: 60

কমা অপারেটর দিয়ে মান রিটার্ন করতে চাইলে সবসময় ব্র্যাকেট () ব্যবহার করা বাধ্যতামূলক।
