Variable (ভেরিয়েবল) হলো একটা পাত্র বা বাক্সের মতো, যার ভেতরে কোনো তথ্য বা ডেটা (যেমন: সংখ্যা, অক্ষর বা দশমিক সংখ্যা) সাময়িকভাবে জমা করে রাখি।

কম্পিউটারের ভাষায়, ভেরিয়েবল হলো কম্পিউটারের মেমোরি বা র‍্যামের (RAM) একটি নির্দিষ্ট জায়গার নাম। যখন কোনো ভেরিয়েবল তৈরি করা হয়, তখন র‍্যামে ওই নামের জন্য একটা জায়গা বুক হয়ে যায় এবং সেখানে ইচ্ছামতো ডেটা রাখতে পারি।

১. ভেরিয়েবল কেমন করে লিখতে হয়? (Syntax)

সি বা সি++ ল্যাঙ্গুয়েজে ভেরিয়েবল লিখতে হলে প্রধানত দুটি জিনিস লাগে:
১. Data Type: আপনি কী ধরণের ডেটা রাখবেন (পূর্ণসংখ্যা, নাকি অক্ষর)।
২. Variable Name: ভেরিয়েবলটির একটি নাম (আইডেন্টিফায়ার)।

Data_Type Variable_Name = Value;
ভেরিয়েবল ব্যাবহার করার পূর্বে তার ডাটা টাইপ বলে দেওয়া লাগবে কি ডাটা স্টোর হবে 

int age = 20;       // int মানে integer বা পূর্ণসংখ্যা। এখানে age হলো ভেরিয়েবল, যার ভেতর ২০ রাখা হয়েছে।
float salary = 450.50; // float মানে দশমিক সংখ্যা।
char grade = 'A';   // char মানে ক্যারেক্টার বা একটি একক অক্ষর।


২. ভেরিয়েবল কতভাবে বা কত উপায়ে লেখা যায়? (Declaring Variables in Different Ways)
কোড লেখার সময় আপনার প্রয়োজন অনুযায়ী ভেরিয়েবলকে বিভিন্ন উপায়ে ডিক্লেয়ার (ঘোষণা) করা যায়। মূলত ৪টি উপায়ে এটি করা যায়:

ক) শুধু ভেরিয়েবল তৈরি করা (Declaration)
এখানে শুধু ভেরিয়েবলটি তৈরি করে রাখা হয়েছে, কিন্তু ভেতরে কোনো মান বা ভ্যালু দেওা হয়নি। পরে প্রয়োজন অনুযায়ী ভ্যালু অ্যাসাইন করা হবে।

int roll; 
float price;
// পরে যেকোনো সময় ভ্যালু দেওয়া যাবে:
roll = 101;


খ) তৈরি করার সাথেই মান দিয়ে দেওয়া (Initialization)
ভেরিয়েবল তৈরি করার লাইনেই তার ভেতরে ডেটা ঢুকিয়ে দেওয়া। এটি সবচেয়ে বেশি ব্যবহৃত হয়।

int speed = 80;
char status = 'Y';


গ) এক লাইনে একই জাতের একাধিক ভেরিয়েবল লেখা (Multiple Variables)
 যদি একই ধরণের (যেমন int) অনেকগুলো ভেরিয়েবল লাগে, তবে বারবার নিচে নিচে না লিখে কমা (,) দিয়ে এক লাইনেই লিখে ফেলা যায়।

int x, y, z; // ৩টি আলাদা ভেরিয়েবল তৈরি হলো
// অথবা একসাথে ভ্যালুসহ:
int a = 10, b = 20, c = 30;


ঘ) মান পরিবর্তন করা যাবে না এমন ভেরিয়েবল (Constant Variable)
যদি একটি ভেরিয়েবলের মান পুরো কোডে ফিক্সড থাকবে, কেউ যেন ভুল করেও তা পরিবর্তন করতে না পারে, তবে তার আগে const কিওয়ার্ড ব্যবহার করতে হয়।

const float PI = 3.1416; // এই PI এর মান নিচে আর কোনো লাইনে পরিবর্তন করা যাবে না।


৩. ভেরিয়েবলের নাম লেখার কিছু জরুরি নিয়ম (Naming Rules)

ভেরিয়েবলের নাম  নিজের ইচ্ছামতো দেওয়া যাবে, তবে সি/সি++ এর কম্পাইলারের কিছু নিয়ম মেনে চলতে হবে:

সংখ্যা দিয়ে শুরু নিষেধ: নামের শুরুতে কোনো নম্বর বা সংখ্যা দেওয়া যাবে না। যেমন: int 1st_pos; (ভুল), কিন্তু int pos_1st; (সঠিক)।

স্পেস বা ফাঁকা জায়গা নিষেধ: নামের মাঝে কোনো স্পেস দেওয়া যাবে না। স্পেসের বদলে আন্ডারস্কোর (_) দিতে পারেন। যেমন: int roll number; (ভুল), কিন্তু int roll_number; (সঠিক)।

কিওয়ার্ড ব্যবহার নিষেধ: সি ল্যাঙ্গুয়েজের নিজস্ব ৩২টি কিওয়ার্ডের কোনোটি (যেমন: int, if, else, while) ভেরিয়েবলের নাম হিসেবে ব্যবহার করা যাবে না।


const ভেরিয়েবলের নাম লেখার সময় অবশ্যই Camel Case, Pascal Case, বা Snake Case ব্যবহার করা যাবে। সি বা সি++ কম্পাইলার আপনাকে এতে কোনো বাধা দেবে না।

কোডাররা সব বড় হাতের অক্ষর (যা আসলে Upper Snake Case) ব্যবহার করে কেবল নিজেদের চেনার সুবিধার জন্য। তবে  কোডিং স্ট্যান্ডার্ড অনুযায়ী যেকোনো কেস স্টাইল বেছে নিতে পারেন।


চার ধরণের কেস স্টাইলের পরিচয়

কেস স্টাইল      (Case Style)লেখার নিয়ম                                      (Rule)উদাহরণ
Camel Case	   প্রথম শব্দ ছোট হাতের, পরের প্রতিটি শব্দের প্রথম অক্ষর বড় হাতের।	maxLimit, taxRate
Pascal Case	   প্রতিটি শব্দেরই প্রথম অক্ষর বড় হাতের হবে।	                    MaxLimit, TaxRate
Snake Case	   সব অক্ষর ছোট হাতের, শব্দগুলোকে আন্ডারস্কোর (_) দিয়ে জোড়া দেওয়া হয়।	max_limit, tax_rate
Upper Snake Case	সব অক্ষর বড় হাতের, শব্দগুলোকে আন্ডারস্কোর (_) দিয়ে জোড়া দেওয়া হয়।	MAX_LIMIT, TAX_RATE


ভেরিয়েবল লেখার এই কেস স্টাইলগুলো (camelCase, PascalCase, snake_case) কোনো নির্দিষ্ট ডাটা টাইপ বা const এর জন্য আটকে নেই। 
সি বা সি++ ল্যাঙ্গুয়েজের যেকোনো ডাটা টাইপ (int, float, char, double ইত্যাদি) কিংবা নিজের তৈরি করা ফাংশন বা স্ট্রাকচারের নামের ক্ষেত্রেও এগুলো ব্যবহার করা যাবে।


১. Snake Case (user_age): এটি C ল্যাঙ্গুয়েজে সবচেয়ে বেশি জনপ্রিয়। সি-এর নিজস্ব লাইব্রেরি ফাংশনগুলোও খেয়াল করলে দেখবেন এই স্টাইলেই লেখা।
২. Camel Case (studentRoll): এটি C++, Java, এবং JavaScript-এ ভেরিয়েবল এবং ফাংশনের নাম লেখার জন্য ওয়ার্ল্ডওয়াইড স্ট্যান্ডার্ড।
৩. Pascal Case (TotalStudents): এটি সি++ বা অন্যান্য ল্যাঙ্গুয়েজে সাধারণত Class (ক্লাস) বা Structure (স্ট্রাকচার) এর নাম দেওয়ার জন্য তুলে রাখা হয়।




Format Specifier (ফরম্যাট স্পেসিফায়ার)

Format Specifier (ফরম্যাট স্পেসিফায়ার) হলো সি ল্যাঙ্গুয়েজের (C Language) একটি বিশেষ কোড বা চিহ্ন, যা কম্পাইলারকে বলে দেয় মেমোরি থেকে কী ধরণের ডাটা ইনপুট নিচ্ছি বা আউটপুটে দেখাচ্ছি।

যখন printf() দিয়ে স্ক্রিনে কিছু দেখাতে চাওয়া হয় বা scanf() দিয়ে কীবোর্ড থেকে ইনপুট নিতে হয়, তখন কম্পাইলারকে বোঝাতে হয় যে ডাটাটি কি কোনো পূর্ণসংখ্যা (Integer), 
নাকি দশমিক সংখ্যা (Float), নাকি কোনো অক্ষর (Character)। এই বোঝানোর কাজটিই করে ফরম্যাট স্পেসিফায়ার।

এটি সবসময় একটি পার্সেন্টেজ চিহ্ন (%) দিয়ে শুরু হয়।

ডাটা টাইপ (Data Type)       ফরম্যাট স্পেসিফায়ার      কী ধরণের ডাটা রাখে 
int                       	%d বা %i	           পূর্ণসংখ্যা (যেমন: 10, -25, 500)
float	                    %f	                   সাধারণ দশমিক সংখ্যা (যেমন: 3.14, 10.5)
double	                    %lf	                   বড় বা নিখুঁত দশমিক সংখ্যা (Long Float)
char	                    %c	                   একটি একক অক্ষর (যেমন: 'A', 'z', '#')
char[] (String)             %s                     পুরো একটি নাম বা বাক্য (যেমন: "Abdullah")



    

সি ল্যাঙ্গুয়েজে (C Language) কিন্তু অন্যান্য মডার্ন ল্যাঙ্গুয়েজের (যেমন C++, Java বা JavaScript) মতো সরাসরি string নামে কোনো ডাটা টাইপ নেই।

সি ল্যাঙ্গুয়েজে ক্যারেক্টারের একটি অ্যারে বা সারি (Array of Characters) তৈরি করে স্ট্রিংয়ের কাজ করা হয়। অর্থাৎ, অনেকগুলো ক্যারেক্টার যখন পরপর এক লাইনে বসে, সেটাই সি-তে স্ট্রিং হয়ে যায়।

১. স্ট্রিং ডিক্লেয়ার বা তৈরি করার নিয়ম
স্ট্রিং তৈরি করতে হলে char ডাটা টাইপ ব্যবহার করে নামের পাশে থার্ড ব্র্যাকেট [ ] দিতে হয় এবং ব্র্যাকেটের ভেতরে সাইজ বলে দিতে হয়।

২. মেমোরি লেভেলে কীভাবে কাজ করে? (The Null Character)
সি ল্যাঙ্গুয়েজে স্ট্রিংয়ের সবচেয়ে গুরুত্বপূর্ণ বিষয় হলো Null Character (\0)। যখন কোনো স্ট্রিং লিখা হয়, কম্পাইলার নিজে থেকে সেই স্ট্রিংয়ের একদম শেষে একটি অদৃশ্য ক্যারেক্টার \0 যোগ করে দেয়। এটি দেখে সি কম্পাইলার বোঝে যে স্ট্রিংটি এখানে শেষ হয়েছে।

সি ল্যাঙ্গুয়েজে সাধারণ ভেরিয়েবলের মতো সরাসরি = চিহ্ন দিয়ে স্ট্রিং Reassign করা যায় না। আর এই কাজটা করার জন্যই ব্যবহার করতে হয় strcpy() (String Copy) ফাংশন।


#include <stdio.h>
#include <string.h> // strcpy() ব্যবহার করার জন্য এটি লাগবে

int main() {
    char name[20] = "Mohammad";
    printf("Old Name: %s\n", name);

    // স্ট্রিং Reassign করা হচ্ছে strcpy দিয়ে
    // নিয়ম: strcpy(কোথায় কপি হবে, কোন লেখাটা কপি হবে);
    strcpy(name, "Abdullah");

    printf("New Name: %s\n", name);

    return 0;
}

সরাসরি = দিয়ে কেন Reassign করা যায় না?
সি-তে স্ট্রিং হলো একটি অ্যারে (Array)। অ্যারের নাম (name) আসলে মেমোরির প্রথম ঘরের একটা ফিক্সড অ্যাড্রেস বা পয়েন্টার নির্দেশ করে।

strcpy() ফাংশন।এটি ব্যবহার করতে হলে কোডের একদম উপরে #include <string.h> হেডার ফাইলটি যুক্ত করতে হয়।

একটি অ্যাডভান্সড সতর্কবার্তা (Security Warning):

strcpy() ফাংশনটি কাজ তো দারুণ করে, কিন্তু এর একটি বড় সিকিউরিটি রিস্ক বা দুর্বলতা আছে, যাকে বলা হয় Buffer Overflow।

ধরুন অ্যারের সাইজ ২০ (char name[20]), কিন্তু strcpy(name, "একটি বিশাল বড় নাম যা ২০ অক্ষরের চেয়ে বেশি"); করে দেওয়া হল।

তখন strcpy সাইজ চেক না করেই মেমোরি ওভাররাইট করে দেবে, যা প্রোগ্রামকে ক্র্যাশ করাতে পারে (বা হ্যাকাররা এর সুযোগ নিতে পারে)।

এই রিস্ক এড়াতে মডার্ন সি কোডিংয়ে strcpy() এর বদলে strncpy() ব্যবহার করার পরামর্শ দেওয়া হয়, যেখানে সর্বোচ্চ কতটি ক্যারেক্টার কপি হবে তা ফিক্সড করে দেওয়া যায়

strncpy(name, "Abdullah", sizeof(name) - 1);
    




#include <stdio.h>
int main() {
    // variable declaration
    int num1;
    int num2;
    float num3;
    double num4;
    char ch;
    // variable initialization
    num1 = 10;
    num2 = 20;
    num3 = 10.2;
    num4 = 10.2535158;
    ch = 'A';
    return 0;
}

#include <stdio.h>
int main() {
    int myNum = 15;

    int myOtherNum = 23;

    myNum = myOtherNum;

    printf("%d", myNum);
    return 0;
}

// Declare Multiple Variables

int x, y, z;
x = y = z = 50;
printf("%d", x + y + z);

// Student data
int studentID = 15;
int studentAge = 23;
float studentFee = 75.25;
char studentGrade = 'B';

// Print variables
printf("Student id: %d\n", studentID);
printf("Student age: %d\n", studentAge);
printf("Student fee: %f\n", studentFee);
printf("Student grade: %c", studentGrade);

// Create integer variables
int length = 4;
int width = 6;
int area;

// Calculate the area of a rectangle
area = length * width;

// Print the variables
printf("Length is: %d\n", length);
printf("Width is: %d\n", width);

printf("Area of the rectangle is: %d", area);



#include <stdio.h>
#include <string.h>

int main() {
    // ১. ডিক্লেয়ারেশন এবং ইনিশিয়ালাইজেশন একসাথে করা (Clean Code)
    char name[30] = "Mohammad";
    int age = 30; 
    float height = 5.2f;
    char village[] = "Changmary";
    char subject[] = "Programming";
    char grade = 'A';
    float cgpa = 3.50f;
    int num1 = 20, num2 = 30;
    
    strcpy(name, "Abdullah Aziz");

    printf("--------- Your Profile ---------\n");
    
    printf("Name     : \"%s\"\n", name); // কোটেশন প্রিন্ট করার জন্য \" ব্যবহার
    printf("Age      : %d Years\n", age);
    printf("Height   : %.1f Feet\n", height);
    printf("Location : %s\n", village);
    printf("Subject  : %s\n", subject);
    printf("Result   : Grade %c (CGPA: %.2f)\n", grade, cgpa);
    
    printf("--------------------------------\n");
    printf("Numbers  : %d, %d\n", num1, num2);

    return 0;
}

১. Variable Initializing:  প্রথমে age = 25 দিয়ে পরে 30 করা হয়েছে। যদি ২৫ এর প্রয়োজন না থাকে, তবে সরাসরি ৩০ ডিক্লেয়ার করা মেমোরি ও সময় বাঁচায়।

২. String Buffer Size: char name[] এভাবে দিলে এটি শুরুর শব্দের সাইজ নেয়। পরে বড় নাম কপি করলে strcpy তে বাফার ওভারফ্লো হতে পারে। তাই char name[30] বা নির্দিষ্ট সাইজ দেওয়া নিরাপদ।

৩. Escaping Quotes: strcpy এর ভেতর ম্যানুয়ালি কোটেশন দিয়েছ। কিন্তু প্রিন্ট করার সময় \"%s\" ব্যবহার করলে কোডটি দেখতে বেশি পরিষ্কার লাগে।

৪. Alignment (প্রান্তিকরণ):  printf এর ভেতর স্পেস ব্যবহার করে ক্লোনগুলো (:) এক লাইনে আনা হয়েছে। এতে আউটপুটটি দেখতে অনেক বেশি প্রফেশনাল লাগে।

৫. f Suffix: float ভ্যালুর শেষে f (যেমন: 3.50f) লেখা ভালো অভ্যাস। না লিখলে সি কম্পাইলার এটিকে double হিসেবে ধরে নিয়ে পরে float-এ কনভার্ট করে।



Token (টোকেন)
সি ল্যাঙ্গুয়েজে Token (টোকেন) হলো একটি প্রোগ্রামের সবচেয়ে ছোট বা ক্ষুদ্রতম একক (Smallest Individual Unit), যা সি কম্পাইলার নিজে চিনতে বা বুঝতে পারে।

সি টোকেন মূলত ৬ প্রকার (Types of Tokens):
সি ল্যাঙ্গুয়েজের যেকোনো কোড মূলত এই ৬টি টোকেন মিলিয়েই তৈরি হয়:

১. Keywords (কিওয়ার্ড)
সি ল্যাঙ্গুয়েজের নিজস্ব সংরক্ষিত বা ফিক্সড শব্দগুলো।

উদাহরণ: int, float, if, else, while, return ইত্যাদি।



২. Identifiers (আইডেন্টিফায়ার)
ইউজার বা প্রোগ্রামার নিজে কোড লেখার সময় ভেরিয়েবল, ফাংশন বা অ্যারের যে নামগুলো দেয়।

উদাহরণ: age, roll_number, main, total_marks ইত্যাদি।



৩. Constants (কনস্ট্যান্ট)
কোডের ভেতরের ফিক্সড বা অপরিবর্তনশীল মানগুলো।

উদাহরণ: 10, 20.5, 'A', 3.1416 ইত্যাদি।


৪. Strings (স্ট্রিং)
ডাবল কোটেশনের (" ") ভেতরে থাকা যেকোনো লেখা বা ক্যারেক্টারের সমষ্টি।

উদাহরণ: "Abdullah", "Learning C", "Hello World" ইত্যাদি।


৫. Operators (অপারেটর)
গাণিতিক বা লজিক্যাল কাজ করার জন্য যে চিহ্নগুলো ব্যবহার করা হয়।

উদাহরণ: +, -, *, /, =, ==, && ইত্যাদি।


৬. Special Characters / Punctuators (বিশেষ চিহ্ন)
কোডের সিনট্যাক্স বা গঠন ঠিক রাখার জন্য ব্যবহৃত চিহ্নগুলো।

উদাহরণ: ( ), { }, [ ], ;, , ইত্যাদি।


int age = 30;

কম্পাইলার যখন এই লাইনটি প্রসেস করবে, সে এটিকে নিচের মতো করে ৪টি টোকেনে ভেঙে ফেলবে:

int Keyword   age Identifier = Operator   22 Constant


    
